<%= render "shared/flash" %>
<div>
  <p><strong>Title:</strong> <%= @book.title %></p>
  <p><strong>Body:</strong> <%= @book.body %></p>
</div>
<div>
  <%= link_to "Edit", edit_book_path(@book), class: "edit" %>
  |
  <%= link_to "Back", books_path, class: "edit" %>
</div>